---
# Bookmarks are their own IndieWeb post kind (#251): a saved URL with an
# optional one-liner. UNLIKE notes they are NOT headless — the single page
# must render, because it is the webmention source the target's receiver
# fetches to find the u-bookmark-of link. robotsdisallow keeps the thin
# pages out of search engines; webmention delivery ignores robots.
cascade:
  params:
    format: bookmark
    robotsdisallow: true
title: "Lesezeichen"
---
