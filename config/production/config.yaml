title: Article Time
baseURL: https://article-time.de/

# buildStats, the hugo_stats.json mount and the cachebuster all live in
# config/_default — see the note there. PurgeCSS reads hugo_stats.json from the
# project root, not through the mount.
module:
  hugoVersion:
    extended: true
    min: "0.115.0"
