title: Angel Crawford
baseURL: https://article-time.de/

# buildStats, the hugo_stats.json mount and the cachebuster all live in
# config/_default — see the note there. (PurgeCSS used to read the stats file
# too; it died with the Bulma pipeline.)
module:
  hugoVersion:
    extended: true
    min: "0.115.0"
