csp:
  default: ["'self'"]
  formaction: ["'self'"]
  framesrc: ["'self'"]
  imgsrc: ["'self'", "https://github.com", "https://*.githubusercontent.com"]
  objectsrc: ["'none'"]
  stylesrc: ["'self'", "'unsafe-inline'", "'unsafe-eval'", "https://unpkg.com"]
  scriptsrc: ["'self'", "'unsafe-inline'", "'unsafe-eval'", "https://unpkg.com"]
  scriptsrcelem: ["'self'", "'unsafe-inline'", "'unsafe-eval'", "https://unpkg.com"]