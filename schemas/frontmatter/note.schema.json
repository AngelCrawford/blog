{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://article-time.de/schemas/frontmatter/note.schema.json",
  "title": "Note Frontmatter",
  "description": "content/notes/**/*.md. A note is headless — one line of text, no single page, no excerpt — so it needs a title and its taxonomy, and nothing else. Everything the article schema knows stays ALLOWED here (growth_stage, withered_*, params.SEO …); only the requirements differ.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "title": {
      "type": "string",
      "description": "Page title. SEO meta title. 60–70 characters max.",
      "minLength": 1
    },
    "slug": {
      "type": "string",
      "description": "Optional URL slug override. Use when title contains umlauts (Ä, Ö, Ü, ß) or other special characters."
    },
    "subtitle": {
      "type": "string"
    },
    "date": {
      "description": "Publication date. Hugo accepts ISO 8601 strings or YAML timestamps; both are valid here. Type intentionally unconstrained so YAML auto-parsed dates (Date objects after gray-matter) don't trip the validator."
    },
    "lastmod": {
      "description": "Last-modification date. Override Hugo's git-based default. Type intentionally unconstrained (see `date`)."
    },
    "publishdate": {
      "description": "Future publish date (only useful when running Hugo on a server). Type intentionally unconstrained (see `date`)."
    },
    "draft": {
      "type": "boolean",
      "default": true,
      "description": "true = excluded from production builds. Flip to false to publish."
    },
    "weight": {
      "type": "integer",
      "default": 0,
      "description": "Sort weight. 1 makes the article sticky on the homepage; other numbers sort articles."
    },
    "categories": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "At least one. The FIRST is the Rubrik shown on the card and in the Steckbrief; the rest are indexing only.",
      "minItems": 1
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "series": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Use the same series name across articles to group them."
    },
    "author": {
      "type": "string",
      "description": "Optional main-author override. Defaults to params.identity.name (site owner) when omitted. Set only for guest posts."
    },
    "coauthors": {
      "type": "array",
      "description": "Optional co-authors rendered as additional p-author h-card blocks. IndieWeb-aligned.",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "url": {
            "type": "string",
            "format": "uri"
          }
        },
        "required": [
          "name"
        ],
        "additionalProperties": false
      }
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Required for articles. Used as the list-view excerpt and SEO description fallback."
    },
    "growth_stage": {
      "type": "string",
      "enum": [
        "seedling",
        "budding",
        "evergreen",
        "withered"
      ],
      "default": "seedling",
      "description": "Digital-garden content maturity. seedling = early/draft, budding = developing, evergreen = mature/maintained, withered = deprecated. If omitted, templates fall back to seedling."
    },
    "withered_date": {
      "type": "string",
      "format": "date",
      "description": "Date when the article was marked as withered. Required when growth_stage == 'withered'. ISO 8601 (YYYY-MM-DD). Populates the withered warning banner (Story 1.4)."
    },
    "withered_reason": {
      "type": "string",
      "maxLength": 280,
      "description": "Optional one-paragraph plain-prose explanation of why the content is deprecated (e.g., 'Framework deprecated, see new article'). No markdown."
    },
    "replacement_url": {
      "type": "string",
      "format": "uri-reference",
      "pattern": "^(https?://|/)",
      "description": "Optional URL or root-relative path to replacement content (e.g., '/articles/new-version/'). Must start with https://, http://, or /. Rendered as a labelled link in the withered banner."
    },
    "params": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "SEO": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "desc": {
              "type": "string",
              "description": "SEO meta description. 120–158 characters. Falls back to summary if empty."
            },
            "keywords": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "1–3 keywords. Auto-derived from tags if empty."
            },
            "canonicalURL": {
              "type": "string",
              "description": "Use when the post is republished from another platform."
            }
          }
        }
      }
    }
  },
  "required": [
    "title"
  ],
  "allOf": [
    {
      "if": {
        "properties": {
          "growth_stage": {
            "const": "withered"
          }
        },
        "required": [
          "growth_stage"
        ]
      },
      "then": {
        "required": [
          "withered_date"
        ]
      }
    }
  ]
}
