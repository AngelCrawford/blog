# Hugo
/resources
/public
/public-*
/themes
hugo_stats.json

# Generated at build time — never committed
/buildDate.txt

# node
node_modules

# Playwright
/playwright-report
/test-results
/playwright/.cache

# Build smoke test scratch (created and cleaned up per-test, but failures may leave residue)
/content/_test_growth_stage_*
/content/articles/_test_growth_stage_*
/content/articles/_test_withered_banner_*
/content/articles/_test_withered_*

# Local debug logs (e.g. `npm test 2>&1 > test-output.log`, npm-debug.log)
*.log
