# Hugo
/resources
/public
/themes
hugo_stats.json

# Generated at build time — never committed
/version.txt
/buildDate.txt

# node
node_modules

# Playwright
/playwright-report
/test-results
/playwright/.cache

# Build smoke test scratch (created and cleaned up per-test, but failures may leave residue)
/content/_test_growth_stage_*
